specVersion: "v1"
serviceId: "dreamscriptor"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "dreamscriptor-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
